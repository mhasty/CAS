import { FileCode2, Upload } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

export const metadata = { title: "UAD XML Appraisal Intake", description: "Legacy URAR XML and UAD 3.6 XML ingestion, normalization, and review." };
const xmlModels = [['Legacy URAR XML','1004 / 1073 / 1025 era UAD XML normalization','Stable'],['UAD 3.6 XML','MISMO 3.6-based dynamic URAR appraisal data ingestion','Priority'],['PDF report cross-check','Compare rendered appraisal PDF vs XML fields','Ready'],['Review rules engine','Caarps + lender + AMC + GSE style checks','Ready']];
export default function XmlPage() { return <AppShell title="XML Appraisal Intake"><div className="space-y-6"><Card><CardContent className="p-6"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><h3 className="text-lg font-semibold">XML appraisal ingestion</h3><p className="text-sm text-slate-500">Read legacy URAR XML and UAD 3.6 XML, normalize fields, compare against PDF, and feed review rules.</p></div><Button><Upload size={16} className="mr-2"/>Upload XML + PDF</Button></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{xmlModels.map(([title,body,state]) => <div key={title} className="rounded-2xl border border-slate-200 p-4"><FileCode2 size={22}/><p className="mt-3 font-medium">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{body}</p><span className="mt-3 inline-flex rounded-full bg-slate-100 px-3 py-1 text-xs text-slate-600">{state}</span></div>)}</div></CardContent></Card><div className="grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><Card><CardContent><h3 className="font-semibold">Normalized appraisal data</h3><div className="mt-4 grid gap-3 md:grid-cols-2">{[['Report type','Dynamic URAR / UAD 3.6'],['Schema','MISMO 3.6'],['Subject GLA','2,184 sq ft'],['Effective date','05/22/2026'],['Opinion of value','$415,000'],['Condition rating','C3'],['Quality rating','Q4'],['Comparable count','6 sales + 2 listings']].map(([k,v]) => <div key={k} className="rounded-2xl border border-slate-200 p-4"><p className="text-xs text-slate-500">{k}</p><p className="mt-1 font-medium">{v}</p></div>)}</div></CardContent></Card><Card><CardContent><h3 className="font-semibold">Cross-checks</h3><div className="mt-4 space-y-3">{[['XML vs PDF','2 mismatches found','Warning'],['UAD completeness','96% complete','Pass'],['Lender overlays','1 missing narrative','Warning'],['Schema validation','Valid XML','Pass']].map(([a,b,c]) => <div key={a} className="flex items-center justify-between rounded-2xl border border-slate-200 p-4"><div><p className="font-medium">{a}</p><p className="text-sm text-slate-500">{b}</p></div><span className={`rounded-full px-3 py-1 text-xs ${c === 'Pass' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700'}`}>{c}</span></div>)}</div></CardContent></Card></div></div></AppShell>; }
