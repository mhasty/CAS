import { Building2, LockKeyhole } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { Card, CardContent } from "@/components/ui/card";

export const metadata = { title: "Lender Appraisal Platform", description: "Fast lender ordering, restricted borrower visibility, secure report delivery, and enterprise appraisal analytics." };
const lenderFeatures = [['Ultra-fast ordering','Create appraisal orders in under 60 seconds with reusable templates and autofill'],['Enterprise reporting','Turn-times, revision rates, SLA metrics, and operational analytics'],['LOS integrations','Encompass, LendingPad, Arive, Byte, and API-based lender workflows'],['Transparent pricing','Clear tech fees with no hidden monthly platform charges'],['Compliance confidence scoring','Automated readiness scoring before delivery'],['Secure lender delivery','PDF + XML delivery with download tracking and audit history']];
const lenderRules = [['Borrower visibility restriction','Borrowers cannot access appraisal reports, review findings, or lender workflow data'],['Lender-controlled delivery','Completed reports deliver only to lender/client-side authorized users'],['Restricted borrower communication','Borrower contact limited to scheduling coordination only when enabled'],['Secure audit controls','All report access, downloads, and deliveries are logged and timestamped']];
export default function LendersPage() { return <AppShell title="Lender Experience"><div className="space-y-6"><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{lenderFeatures.map(([title,body]) => <Card key={title}><CardContent><Building2 size={22}/><p className="mt-3 font-medium">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{body}</p></CardContent></Card>)}</section><Card><CardContent className="p-6"><h3 className="text-lg font-semibold">Restricted borrower visibility model</h3><p className="mt-1 text-sm text-slate-500">The lender/client is the true client relationship. Borrower-facing interaction is intentionally restricted and controlled.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{lenderRules.map(([title,body]) => <div key={title} className="rounded-2xl border border-slate-200 p-5"><LockKeyhole size={18}/><p className="mt-3 font-medium">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{body}</p></div>)}</div></CardContent></Card></div></AppShell>; }
